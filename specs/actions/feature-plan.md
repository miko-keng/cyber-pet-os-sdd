# Feature Plan: Interaction Layer

- Create a `ButtonGroup` component.
- Each button calls a `handleAction(type)` function.
- Function must check for constraints before updating state.
- Trigger a small CSS animation (e.g., "pulse") when an action succeeds.